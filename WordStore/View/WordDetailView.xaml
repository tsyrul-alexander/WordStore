<?xml version="1.0" encoding="utf-8" ?>
<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
			 xmlns:converter="clr-namespace:WordStore.Converter"
			 xmlns:view="clr-namespace:WordStore.View"
             x:Class="WordStore.View.WordDetailView"
            BindingContext="{Binding Path=WordDetail,Source={StaticResource Locator}}">
    <ContentView.Content>
		<Grid RowDefinitions="50, 1*, 1*"
				IsVisible="{Binding Word, Converter={StaticResource IsNullConverter}, ConverterParameter={converter:Boolean Value=True}}">
			<Label Grid.Row="0" Text="{Binding Word.Value, StringFormat='Word: {0}'}" Style="{StaticResource HeaderLabel}"/>
			<StackLayout Grid.Row="1">
				<StackLayout Orientation="Horizontal">
					<Label Text="Translations:" Style="{StaticResource HeaderLabel}"/>
					<ImageButton Source="add_icon.png" HorizontalOptions="Center"
						VerticalOptions="Center"/>
				</StackLayout>
				<view:ListEditView ItemsSource="{Binding Word.Translations}" BackgroundColor="Beige"></view:ListEditView>
			</StackLayout>
			<StackLayout Grid.Row="2">
				<Label Text="Examples" Style="{StaticResource HeaderLabel}"/>
				<view:ListEditView ItemsSource="{Binding Word.Examples}" BackgroundColor="Beige"></view:ListEditView>
			</StackLayout>
        </Grid>
    </ContentView.Content>
</ContentView>