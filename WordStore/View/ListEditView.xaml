<?xml version="1.0" encoding="utf-8" ?>
<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
			 xmlns:model="clr-namespace:WordStore.Model.View"
             x:Class="WordStore.View.ListEditView">
	<ContentView.Content>
		<StackLayout>
			<ListView x:Name="List" ItemsSource="{Binding Path=ItemSource}">
				<ListView.ItemTemplate>
					<DataTemplate x:DataType="model:ListItemView">
						<ViewCell>
							<Grid ColumnDefinitions="*, 60, 60">
								<Label Grid.Column="0" Text="{Binding Value}" Style="{StaticResource ListItemLabel}"/>
								<ImageButton Grid.Column="1" Source="edit_icon.png" HorizontalOptions="Center"
									VerticalOptions="Center"/>
								<ImageButton Grid.Column="2" Source="delete_icon.png" Margin="0" Padding="0" Aspect="AspectFit"/>
							</Grid>
						</ViewCell>
					</DataTemplate>
				</ListView.ItemTemplate>
			</ListView>
		</StackLayout>
	</ContentView.Content>
</ContentView>